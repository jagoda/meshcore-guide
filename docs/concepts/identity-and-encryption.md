# Identity and Encryption

<!-- TODO -->

The key model: how each node generates a key pair, how MACs authenticate
packets, and why MeshCore needs no central server for trust. Conceptual
overview — byte-level details are at docs.meshcore.nz.
