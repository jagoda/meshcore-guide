# Payload Types Tour

<!-- TODO -->

A guided tour of the payload type space: what each payload type is *for*
and when it appears on the wire. Byte-level payload tables are in the
[Payloads spec](https://docs.meshcore.nz/payloads/) on docs.meshcore.nz.
