# Adverts Deep Dive

<!-- TODO -->

How node adverts are constructed and flooded, what triggers a re-advert, and
how flood adverts differ from targeted adverts. Byte-level fields are in the
[Payloads spec](https://docs.meshcore.nz/payloads/) and
[Number Allocations](https://docs.meshcore.nz/number_allocations/) on docs.meshcore.nz.
