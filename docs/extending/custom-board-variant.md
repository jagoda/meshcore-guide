# Custom Board Variant

<!-- TODO -->

Adding a new board or hardware variant to MeshCore: the `boards/` + `variants/`
convention, what the Board interface requires, and how the build system picks up
a new variant.
