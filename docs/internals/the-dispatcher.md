# The Dispatcher

<!-- TODO -->

The `Dispatcher` class: the central scheduling loop, how it drives the radio,
`PacketManager`, and timed callbacks. Why the dispatcher pattern keeps the
firmware single-threaded and predictable.
