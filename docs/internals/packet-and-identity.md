# Packet and Identity

<!-- TODO -->

The `Packet` class as the wire object (serialization, deserialization, field
access) and the `Identity` / `LocalIdentity` classes as the crypto primitives
(key generation, signing, verification).
