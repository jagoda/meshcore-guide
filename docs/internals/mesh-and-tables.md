# Mesh and Tables

<!-- TODO -->

The `Mesh` class and `MeshTables`: how the routing brain tracks known nodes,
path hashes, and contact records. The relationship between the in-memory tables
and what gets persisted.
