# Example Apps

<!-- TODO -->

Tour of the six `examples/` applications: `companion_radio`, `simple_repeater`,
`simple_room_server`, `simple_sensor`, `simple_secure_chat`, and `kiss_modem`.
Each example as a template and extension anchor.
