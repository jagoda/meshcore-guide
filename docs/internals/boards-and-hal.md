# Boards and HAL

<!-- TODO -->

The hardware abstraction layer: `arch/`, `boards/`, `variants/`, and the
`*Board` helper classes. How adding a new board wires into the build system
and what the Board interface requires.
