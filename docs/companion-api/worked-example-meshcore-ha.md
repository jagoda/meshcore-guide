# Worked Example — meshcore-ha

<!-- TODO -->

The `meshcore-ha` Home Assistant integration as a real-world consumer of the
Companion API. Shows how an integration connects, subscribes to events, sends
commands, and maps MeshCore data to Home Assistant entities.
